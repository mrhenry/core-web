(function(undefined) {
if (!("console"in self&&"profiles"in self.console
)) {
// console.profiles
console.profiles = function profiles() {};
}}).call('object' === typeof window && window || 'object' === typeof self && self || 'object' === typeof global && global || {});
